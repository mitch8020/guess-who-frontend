# API / realtime endpoints
PORT=1073
VITE_API_BASE_URL=http://localhost:1000/api
VITE_WS_BASE_URL=ws://localhost:1000/ws

# Rollbar (optional in local; app works without token)
VITE_ROLLBAR_CLIENT_TOKEN=
VITE_ROLLBAR_ENV=development
VITE_ROLLBAR_CODE_VERSION=dev

# Optional: enable TanStack devtools Vite plugin in local dev
# Set to true only if port 42069 is free

VITE_ENABLE_TANSTACK_DEVTOOLS=false
